# Tracked changes: draft4 → draft5

`main.pdf` in this folder is the current report with every text change marked
against the previous version.

- **Deleted text** — red, struck through
- **Inserted text** — blue, underlined

Baseline is `draft4_03_09_26` (the version last sent to the supervisor);
revision is `draft5_06_09_26` (current). Produced with latexdiff 1.3.2.

Counts: 49 insertions, 54 deletions.

## Changes visible as markup in the PDF

| Where | Change | Prompted by |
|---|---|---|
| 2.2 | "Literature Review" → **Related Works** | chapter already carries that name |
| 4.5 | "Experimentation" → **Implementation** | supervisor |
| Ch. 4 title | → **Implementation and Results** | supervisor |
| 4.6 | restructured into **Results** with Quantitative / Qualitative / Analysis of the Results beneath it | supervisor |
| Every section lacking one | 2–3 line lead-in paragraph added | supervisor |
| 3.8 | conclusion expanded from ~110 to 307 words | supervisor |
| 4.6 lead-in | "…rather than simply restating it" removed | supervisor |
| 4.9.2, 4.9.4, 7.1 | paragraphs tightened so no page opens with a stranded 2–3 line fragment | supervisor |
| Related Works | YOLOv3 and YOLOv4 arXiv citations removed | own decision |

## Changes NOT visible as markup

latexdiff marks text only, so these do not appear in the PDF:

- **Fig. 4.2** — moved to a log10 count axis (the red bar was unreadable on a
  linear scale); the zero-instance ≥96 px bin removed rather than labelled 0%;
  four distinct colours and four hatch directions.
- **Fig. 4.3** — hatch colours darkened, baseline bar hatched, delta labels
  given white backing.
- **All bar charts** — colour *and* pattern fill, so series stay separable in
  greyscale print.
- **Line spacing** — `\setstretch{1.43}` (20.7 pt at 12 pt base), matching the
  template's measured pitch.
- **Widow/orphan penalties** raised to 10000.
- **DOI and URL styling** set to the body font rather than typewriter.

Figure sources: `tikz/fig_size_distribution.tex`, `tikz/fig_ablation_waterfall.tex`.
Style: `thesisstyle.sty`.

## Rebuilding

```
bash build.sh
```

`main.tex` here is a flattened single file generated by latexdiff, not the
modular source. Edit `../draft5_06_09_26/` for real changes and regenerate
this folder from it.
