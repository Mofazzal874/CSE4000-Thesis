# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
pip-wheel-metadata/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Jupyter Notebooks
.ipynb_checkpoints/
*/.ipynb_checkpoints/*

# Model Weights & Data
*.pt
*.pth
*.onnx
*.engine
*.h5
*.keras
*.ckpt
*.safetensors
*.tflite
*.pb
*.weights

# Archives & Compressed Files
*.zip
*.rar
*.7z
*.tar
*.gz
*.iso
*.bz2
*.xz

# Large Data & Documents
*.tsv
*.pptx
*.ppt
*.doc
*.docx
*.pdf
# Keep allowed files
!*.txt
!*.csv
!*.xls
!*.xlsx

# Dataset directories
*dataset*/
*Dataset*/

# Directories with heavy artifacts
results/**/*.png
results/**/*.jpg
runs/
wandb/
mlruns/
# Keeping results folder itself visible to track CSVs
# results/
YOLO11m_vs_YOLO11m_ECA_working_dir_of_kaggle/
5-2-26/kaggle/

# VS Code
.vscode/

# OS
.DS_Store
Thumbs.db

# Drone footage (real DJI Air 3S 4K videos + inference outputs -- large / regenerable)
Drone Shoot/
drone_inference_out/
*.mp4
*.MP4
*.mov
*.MOV
*.avi
*.mkv

# Defense demo bundle (kept on disk, excluded from novelty branches - 4k+ files)
Defense/demo/

# Large detection / prediction JSON dumps (regenerable via the eval harness --save-preds;
# keep the small s1_eval_*.json summaries, drop the multi-MB per-detection dumps)
*_preds.json
*_coco_dets.json
# per-detection (score,correct) arrays for significance -- binary, regenerable by re-running 18_s1_eval
*_scorecorr.npz

# RealDisaster extracted frames (images regenerable from source videos via the extractor;
# keep manifests + annotation JSON, drop the frame images)
RealDisaster/**/*.jpg
RealDisaster/**/*.jpeg
RealDisaster/**/*.png

# Third-party Claude Code skills (vendored from public repos into .claude/skills/;
# ~330KB of upstream code, re-installable from E:\Claude Skills -- not thesis history).
# NOTE: only skills/ is ignored; .claude/settings.json stays tracked.
.claude/skills/
